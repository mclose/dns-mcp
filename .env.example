# DNS MCP Server Configuration
# stdio mode has no required env vars — this file is a placeholder
# for any future configuration needs.

# Example: override DNS resolver used by dnspython (not currently wired in)
# DNS_RESOLVER=8.8.8.8
