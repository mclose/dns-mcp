# Fail2Ban jail for DNS MCP proxy
# Install Location: /etc/fail2ban/jail.d/dns-mcp.conf

[dns-mcp]
enabled = true
port = http,https
filter = dns-mcp
logpath = /var/log/dns-mcp-access.log
maxretry = 10
findtime = 300
bantime = 3600
action = iptables-allports[name=dns-mcp]
